# Task: none

Status: none. No plan is active.

This file is the task plan slot. Start the next non-trivial task by replacing this content with a filled copy of `tasks/TEMPLATE.md`, per `AGENTS.md` Rule 1. One active plan at a time.

The file is local by default (gitignored during setup), so each machine carries its own active plan.
